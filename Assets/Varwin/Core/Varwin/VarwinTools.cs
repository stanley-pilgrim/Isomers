using UnityEngine;
using Varwin.WWW;

namespace Varwin.Editor
{
    [RequireComponent(typeof(AssetBundleManager))]
    [RequireComponent(typeof(DebugLauncher))]
    [RequireComponent(typeof(RequestManager))]
    public class VarwinTools : MonoBehaviour
    {
    }
}
